# 스파이크: OCR과 복합 예외 발화 파싱

## 위험한 가정

영수증 OCR 텍스트와 “술 안 마신 2명 빼줘” 같은 복합 구어체 예외 발화가 섞여도 `gemini-3.6-flash`가 `SettlementRequestContext` Pydantic 스키마로 요구 품질 이상 정확하게 구조화할 수 있다고 가정한다.

## 시간 제한

- **Time-box:** 1일
- **중단 조건:** 최대 6시간 안에 스키마 응답, 라벨 데이터, 판정 스크립트를 준비하지 못하면 범위를 OCR 텍스트 정규화와 파싱으로 축소한다.

## 사전 확정 성공 조건

- **평가 단위:** `ItemException`과 차수 정보가 포함된 비정형 발화 30건
- **판정:** `total_participants_count`, `rounds_info`, `exceptions` 구조가 사람이 작성한 정답 라벨과 건별 완전 일치해야 통과
- **합격선:** 30건 중 24건 이상, 즉 일치율 80% 이상

## 측정 및 오류 분류

- 실측 일치율, 필드별 누락률, 전체 실패 건수를 기록한다.
- 오류 유형을 수량 오인, 제외 인원 누락, 차수 혼합, 금액 오인으로 분류한다.
- API 응답이 JSON이 아니거나 스키마를 위반하면 실패로 기록한다.

## 결과 및 판단

실험 종료 후 아래 표를 채운다.

| 항목 | 결과 |
|---|---|
| 통과 건수 / 30 | 미측정 |
| 일치율 | 미측정 |
| 주요 오류 유형 | 미측정 |
| 가정 판단 | 보류 |

## 스펙 반영 규칙

80% 미만이거나 특정 필드의 불확실성이 반복되면 해당 필드는 기본값 `null` 또는 `[]`으로 두고 사용자 재질문을 요구한다. 이 후속 대화 규칙은 [SPEC.md](../SPEC.md)의 파서 인터페이스와 수용 기준에 반영한다.